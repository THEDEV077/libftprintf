#include "ft_printf.h"
#include <stdio.h>

int main()
{
    // close(1);
    int ret = ft_printf(NULL);
    // fprintf(stderr, "%d", ret);
    // ret = printf("%p", NULL);
    // printf("%d", ret);
}

